# NOTE: version floors, not exact pins. Calibration internals (and therefore
# ECE/Brier and the committed outputs/*) can shift slightly between scikit-learn
# releases. For byte-identical reproduction, pin exact versions in your env.
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
streamlit>=1.31
plotly>=5.18
